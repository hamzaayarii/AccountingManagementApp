pipeline {
    agent any

    stages {
        
        stage('Unit Test') {
            steps {
                echo 'Running unit tests'
            }
        }

        stage('Build application') {
            steps {
               echo 'Building application'
            }
        }
    }
}
